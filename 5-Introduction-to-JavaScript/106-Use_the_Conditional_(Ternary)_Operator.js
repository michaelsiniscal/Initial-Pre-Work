// Learned how to use conditional (ternary) operator to test a true/false condition and return a "canned" statement for either result. Then used ternary operator to return result if two values are equal or not.

function checkEqual(a, b) {
  return a == b ? true : false ;
}

checkEqual(1, 2);
