// Used simplied return consisting of input arguments with test operator between to get true/false reading

function isLess(a, b) {
  // Fix this code
    return a <= b;
  }


// Change these values to test
console.log(isLess(10, 10));
