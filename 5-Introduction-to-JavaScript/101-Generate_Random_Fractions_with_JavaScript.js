// Used Math.random to generate random decimals

function randomFraction() {
  // Only change code below this line.
  var number = 0;
  while (number >= 0) {
    number = Math.random();
  }

  return number;
  // Only change code above this line.
}
